# Aldi Cart

A PWA for scanning Aldi items in-store and tracking your shopping total in real time. Point your phone camera at a barcode, see the price, add it to the cart, watch the subtotal climb as you walk the aisles.

## Features

- **Three screens**: Cart, Scan, Search. Bottom-nav UI, no clutter.
- **Barcode scanner** with a three-engine fallback chain so it works on every device:
  1. `BarcodeDetector` (native) — fastest, all formats, on Android Chrome.
  2. `web-wasm-barcode-reader` (ZBar compiled to WASM) — near-native for EAN-13 on iOS Safari.
  3. `@zxing/browser` — universal fallback. Slower, but covers every format.
- **3,296 Aldi products** + **4,202 Open Food Facts matches** pre-loaded into SQLite.
- **Offline mode**: the full catalogue + EAN lookup table dump into IndexedDB on first sync. The scan flow then resolves any EAN without a network round-trip. A "Sync now" banner shows the cache freshness.
- **HTTPS on port 7778** for iOS camera access on the homelab LAN IP.
- **Cart persistence** server-side, keyed by UUID, sharable via URL.

## Quick start

```bash
npm install                  # also copies WASM assets to public/
npm run dev                  # http://localhost:3000 (no camera on LAN)

npm run dev:https            # https://localhost:7778 + http://localhost:3000
                             # (camera works on iOS via https://192.168.68.55:7778)

# Generate the self-signed cert if certs/ is missing
npm run https:gen
```

First-time setup for the catalogue (this takes ~30s):

```bash
npm run sync:all             # pull Aldi + Open Food Facts, build matches
```

To run end-to-end tests:

```bash
npm run dev:https            # in one terminal
npm run test:e2e             # in another
```

## Project structure

```
src/
  app/
    api/                     # route handlers (cart, ean, search, catalogue, …)
    page.tsx                 # main entry, three-tab layout
    layout.tsx
    globals.css              # Tailwind 4 + Aldi theme
  components/
    Scanner.tsx              # three-engine scanner
    CartView.tsx
    SearchView.tsx
    NavBar.tsx
    SyncBanner.tsx           # offline catalogue sync UI
  lib/
    db.ts                    # better-sqlite3 handle
    proxy.ts                 # Webshare proxy pool for sync scripts
    aldi.ts                  # Aldi API helpers
    format.ts                # product formatter
    client/
      catalogue.ts           # IndexedDB offline cache
data/
  aldi.db                    # SQLite (gitignored, regenerated by sync:all)
certs/                       # self-signed TLS (gitignored, run https:gen)
public/
  a.out.js, a.out.wasm       # ZBar WASM (gitignored, copied by postinstall)
scripts/
  sync-aldi.ts               # pull Aldi catalogue
  sync-off.ts                # pull Open Food Facts
  match.ts                   # build EAN -> Aldi SKU matches
  gen-https.sh               # generate self-signed cert
  copy-wasm.sh               # copy ZBar WASM to public/
server.ts                    # custom Next.js server with HTTPS
```

## Scanner details

The scanner picks the best engine per device at startup:

| Device | Engine | Why |
|---|---|---|
| Android Chrome | `BarcodeDetector` | Native, fast, supports DataMatrix (Aldi produce stickers) |
| iOS Safari | ZBar WASM | Safari doesn't have BarcodeDetector; WASM is near-native for EAN-13 |
| Anything else | ZXing | Universal fallback, slowest |

The `Scanner.tsx` component owns a `<video>` element and tries engines in order, falling back on init failure. Detected EANs are deduped with a 1.5s lockout so a barcode held in front of the camera doesn't fire repeatedly.

## Offline mode

The first time you visit the app, hit "Sync now" in the banner. That fetches `/api/catalogue/dump` (1.3 MB, ~70ms on LAN) and stores all 3,296 products + 4,218 EAN lookups in IndexedDB. The scan flow then resolves EANs locally.

The cart is server-side and **not** cached. If you're offline and try to add an item, the request will fail. This is intentional — a stale local cart with wrong prices is worse than knowing the add failed.

To re-sync (e.g. after a new `npm run sync:all`):

1. Tap the "Offline ready" banner to expand it.
2. Tap "Sync now".

The banner shows cache freshness in relative time ("synced 5 min ago").

## HTTPS / iOS

iOS Safari blocks `getUserMedia` on plain HTTP unless the host is `localhost`. To use the camera on the LAN IP (192.168.68.55), HTTPS is required.

```bash
npm run https:gen            # one-time, writes certs/cert.pem and certs/key.pem
npm run dev:https            # https://192.168.68.55:7778
```

On your iPhone:
1. AirDrop `certs/cert.pem` to the device and install the profile.
2. Go to **Settings → General → About → Certificate Trust Settings** and toggle it on.
3. Visit `https://192.168.68.55:7778`.

The cert is good for 365 days and covers `localhost`, `127.0.0.1`, and the LAN IP.

## Privacy

The scan flow makes no third-party API calls. All lookups go to your own server. The only outbound network calls happen during `npm run sync:aldi` and `npm run sync:off`, which proxy through Webshare.

## Deploying to Dokploy

The repo ships a `Dockerfile` (single-container image) and `docker-compose.yml`
(Dokploy's preferred "Docker Compose" source type). The database is an
external Postgres container in the same Dokploy network — the app reads
`DATABASE_URL` and bootstraps the schema on first request via
`src/lib/db.ts`.

### Required env vars (set in Dokploy's service "Environment" tab)

| Var | Required? | Notes |
|-----|-----------|-------|
| `DATABASE_URL` | **REQUIRED** | Postgres connection string for the external database container. Format: `postgresql://user:pass@host:5432/db`. The host must resolve on the Dokploy internal network (e.g. `postgresql://postgres:...@database-grocerycart-l0kdmu:5432/grocery-cart`). `src/lib/db.ts` throws on startup if this is missing. |
| `PROXY_URL` | optional | Webshare rotating proxy URL. Only needed if you run `npm run sync:off` to refresh the OFF EAN matches. Format: `http://user:pass@p.webshare.io:80`. The running app server does not read this — only the sync script does. |

### Volumes (named, persist across deploys)

- `aldi-cart-certs` → `/app/certs` (self-signed TLS cert for LAN use; not needed if Dokploy terminates HTTPS at Traefik)

### One-time setup

1. In Dokploy, create a service, type "Docker Compose", point it at this repo
2. The compose file uses Traefik labels automatically — point your domain at the service
3. Set `DATABASE_URL` in the env-var UI to the internal Postgres URL; set `PROXY_URL` (or leave empty)
4. Deploy. The first build takes ~3-5 minutes; subsequent builds are faster (layer cache).
5. The container's `/api/health` endpoint is used for the healthcheck; Traefik waits for it to return 200 before routing traffic.
6. DB schema is bootstrapped on first request via the `src/lib/db.ts` schema init (idempotent `CREATE TABLE IF NOT EXISTS`).

### Rotating the Webshare proxy

Update `PROXY_URL` in Dokploy's env-var UI and redeploy. The next time you run
`npm run sync:off` (from the host or a sidecar container), the new proxy is used.
